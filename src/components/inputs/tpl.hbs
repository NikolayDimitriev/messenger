{{#each inputs}}
	<div class="field">
		<label class="field__label" for="{{id}}">{{placeholder}}</label>
		<input class="field__input" placeholder="{{placeholder}}" id="{{id}}" name="{{name}}" type="{{inputType}}"></input>
	</div>
{{/each}}
